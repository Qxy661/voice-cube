# 声纹魔方 Voice Cube v3 - 依赖清单
# 核心依赖（必需）
gradio>=4.0.0
numpy>=1.24.0
scipy>=1.11.0
librosa>=0.10.0
soundfile>=0.12.0
matplotlib>=3.7.0

# 可选依赖（AI克隆功能，无则使用DSP模拟模式）
# 安装: pip install torch>=2.0.0 torchaudio>=2.0.0
# torch>=2.0.0
# torchaudio>=2.0.0

# 可选依赖（RVC ONNX 实时推理）
# pip install onnxruntime>=1.17.0 soundfile>=0.12.0
# onnxruntime>=1.17.0

# 可选依赖（麦克风录音，Windows需先安装portaudio）
# pyaudio>=0.2.13

# 开发依赖（可选）
# pip install -e ".[dev]"
# pytest>=7.0
# pytest-cov>=4.0
# ruff>=0.1.0
